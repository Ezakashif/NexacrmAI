# Optional PaaS deploy hints shared by App/Worker/Cron services from this repo.
# Configure HTTP healthchecks on the web service only, so queue/cron
# processes are not probed on /up.

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
